apply plugin: 'java'

dependencies {
    testCompile files('libs/junit-4.11.jar', 'libs/hamcrest-core-1.3.jar')
}

buildscript {
    dependencies {
        classpath 'com.android.tools.build:gradle:0.10.+'
    }
    repositories {
        mavenCentral()
    }
}